# Extra packages for the evaluation scripts, benchmark and diagrams.
# The detector itself only needs requirements.txt.
-r requirements.txt
pandas>=2.0
pyarrow>=14.0
lightgbm>=4.0
psutil>=5.9
matplotlib>=3.8
